<%= form_with(model: @insta, local: true ,url: choose_new_or_edit) do |form| %>
  <% if @insta.errors.any? %>
    <div id="error_explanation">
      <h2><%= @insta.errors.count %>件のエラーがあります。</h2>
      <ul>

      <% @insta.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="insta_image">
    <%= form.label :image %>
    <%= form.file_field :image, id: :feed_image %>
    <%= form.hidden_field :image_cache %>
  </div>

  <div class="insta_title">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>

<%= link_to "Back", instas_path %>